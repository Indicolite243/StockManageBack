from django.apps import AppConfig
import threading


class AccountConfig(AppConfig):
    """
    ???????
    ?????????????? MyAppConfig?
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.account'
    label = 'account'

    def ready(self):
        # ???????????????????
        from apps.utils.xt_init import init_xtdatacenter_once
        from apps.account.snapshot_scheduler import start_snapshot_scheduler

        # ???????????????????
        # init_xtdatacenter_once ???????????????
        thread = threading.Thread(target=init_xtdatacenter_once, daemon=True)
        thread.start()
        start_snapshot_scheduler()
